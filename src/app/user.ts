import { Building, Housing } from "./type";

export class User {
    static username;
    static password;
    static realname;
    static id;
    name
}

export class GlobalVar {
    static current_housing: Housing;
    static current_building: Building;
}